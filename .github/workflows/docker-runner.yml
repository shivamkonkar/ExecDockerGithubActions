name: Build and Run Docker Container

on:
  workflow_dispatch:

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Test Engine
        uses: shivamkonkar/ExecDockerGithubActions@v0
        with:
          storage_id: ${{ secrets.STORAGE_ID }}
